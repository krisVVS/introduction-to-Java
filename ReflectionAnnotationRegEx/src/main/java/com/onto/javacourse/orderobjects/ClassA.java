package com.onto.javacourse.orderobjects;

/**
 * ClassA is a subclass of {@link ParentClass} annotated with {@link Order}.
 * It has an order value of 2, used for sorting purposes.
 */
@Order(2)
public class ClassA extends ParentClass {
}
