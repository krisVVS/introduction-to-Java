package com.onto.javacourse.orderobjects;
/**
 * ClassC is a subclass of {@link ParentClass} annotated with {@link Order}.
 * It has an order value of 3, used for sorting purposes.
 */
@Order(3)
public class ClassC extends ParentClass{
}
