package com.onto.javacourse.orderobjects;
/**
 * ClassB is a subclass of {@link ParentClass} annotated with {@link Order}.
 * It has an order value of 1, used for sorting purposes.
 */
@Order(1)
public class ClassB extends ParentClass{
}
